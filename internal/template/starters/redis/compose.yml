services:
  redis:
    image: redis:${REDIS_TAG:-7-alpine}
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    expose:
      - "6379"
volumes:
  redisdata: {}
